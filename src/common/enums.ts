export enum AuthProvider {
  GOOGLE = 'google',
}

export enum PieceType {
  BOOK = 'book',
  MOVIE = 'movie',
}

export enum ProgressStatus {
  PENDING = 'pending',
  IN_PROGRESS = 'in-progress',
  FINISHED = 'finished',
}
